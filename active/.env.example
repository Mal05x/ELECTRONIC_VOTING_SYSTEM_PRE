# Active liveness service configuration
# Copy to .env and fill in real values

# REQUIRED: Must match the LIVENESS_SECRET in evoting-fixed/.env
# Generate with: openssl rand -hex 32
LIVENESS_SECRET=your_shared_secret_here

# Optional: Override default port (5002)
PORT=5002
